{
  "name": "heroku-cra-node",
  "version": "3.0.0",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "start": "nodemon server",
    "build": "cd client/ && yarn && yarn build"
  },
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ],
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.7"
  },
  "devDependencies": {}
}
